package main

import "testing"

// TestInsertClinic verify that the clinic is in the DB
func TestInsertClinic(t *testing.T) {
	// call create clinic
	// verify it's in the db

	// err := InsertClinic("test-data/clinic.json")
	// call the db
	// clinic, err = FindClinicInDB()

	// if clinic.ClinicFullName != "Health Habits" {
	// 	t.Errorf("Clinic stored in the DB is not the same", err)
	// }

	// if err != nil {
	// 	t.Errorf("Error inserting a clinic", err)
	// }
}
